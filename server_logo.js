export const serverStartLogo = (port, path) => {
  return (
    `     ================================================
    /                                                \\
   | 🚀 Server ready at http://localhost:${port}${path} |
    \\                                                /
     ================================================
   `
  )
};
